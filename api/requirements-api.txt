# Extra dependencies for the FastAPI layer (on top of what the solvers
# already need: psycopg2, ortools, etc.). Pin loosely; these are stable.
#
#   pip install -r requirements-api.txt
#
fastapi>=0.110
uvicorn[standard]>=0.29
PyJWT>=2.8
bcrypt>=4.1
# pydantic v2 comes in automatically with modern FastAPI; listed for clarity.
pydantic>=2.6
